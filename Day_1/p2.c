//add two numbers
#include<stdio.h>
int main() {              // start  of the prog
    int a = 10, b = 20;     // declearing int variable for a 
    int sum = a + b;  // Storing the a and b inside sum variable 
    printf("Sum = %d", sum); // Printing the sum (Used %d bcz it is a integer type )
    return 0;
}

