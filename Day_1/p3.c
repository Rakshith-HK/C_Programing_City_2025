// USER input 

#include <stdio.h> 
 
int main() {  // Start Point of prog
    int a, b; //Inilizeing the variables 
    scanf("%d %d", &a, &b); // Taking user input 
    printf("Sum = %d", a + b); // Printing sum 
    return 0;
}